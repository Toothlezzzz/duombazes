@model stationary_shop.Models.Customers

@* this will also be used as page title in main layout *@
@{ ViewData["title"] = "Order deletion"; }

@* content header *@
<div class="main-content-header">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a 
			href="@Url.Action("Index")" 
			class="btn btn-outline-primary btn-sm"
		>
			<i class="fa-solid fa-angles-left"></i> Back to the list
		</a>
	</span>
</div>

@* content itself *@
<div class="main-content-ced">
@using( Html.BeginForm(FormMethod.Post, new { @class="entity-d" }) )
{
	@Html.AntiForgeryToken()

	<fieldset>
		<legend class="confirm-msg">Do you really want to remove this entity?</legend>

		<dl>
			<dt>@Html.DisplayNameFor(m => m.Id)</dt>
			<dd>@Html.DisplayFor(m => m.Id)</dd>

			<dt>@Html.DisplayNameFor(m => m.Name)</dt>
			<dd>@Html.DisplayFor(m => m.Name)</dd>

			<dt>@Html.DisplayNameFor(m => m.Surname)</dt>
			<dd>@Html.DisplayFor(m => m.Surname)</dd>

            <dt>@Html.DisplayNameFor(m => m.Mail)</dt>
			<dd>@Html.DisplayFor(m => m.Mail)</dd>

            <dt>@Html.DisplayNameFor(m => m.PhoneNumber)</dt>
			<dd>@Html.DisplayFor(m => m.PhoneNumber)</dd>

            <dt>@Html.DisplayNameFor(m => m.BirthDate)</dt>
			<dd>@Html.DisplayFor(m => m.BirthDate)</dd>

            <dt>@Html.DisplayNameFor(m => m.LoyaltyProgramMembership)</dt>
			<dd>@Html.DisplayFor(m => m.LoyaltyProgramMembership)</dd>
		</dl>

		@if( ViewData["deletionNotPermitted"] != null )
		{
			<div class="fail-msg">
				<i class="fa-solid fa-circle-exclamation"></i> This customer cannot be deleted.
			</div>
		}
		else
		{
			<div class="buttons">
				<button
					formaction="@Url.Action("DeleteConfirm", new {id = Model.Id})"
					class="btn btn-danger"
					style="white-space: nowrap;"
				><i class="fa-solid fa-trash"></i> Remove</button>
			</div>			
		}
	</fieldset>
}
</div>

@* content footer *@
<div class="main-content-footer">
	<span class="title"></span>
	<span class="actions">
		<a 
			href="@Url.Action("Index")" 
			class="btn btn-outline-primary btn-sm"
		>
			<i class="fa-solid fa-angles-left"></i> Back to the list
		</a>
	</span>
</div>